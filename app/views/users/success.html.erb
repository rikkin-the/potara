<script>
  function copyUrl() {
    const input = document.createElement("input");
    input.value = '<%= login_url %>';
    document.body.appendChild(input);
    input.select();
    document.execCommand("copy");
    input.remove();
    const copyButton = document.getElementById('copyButton')
    copyButton.textContent = 'コピーされました'
    copyButton.style.setProperty('background-color', 'lightblue', 'important');
  }
</script>

<div class="main__container">
  <div class="general_field">
    <h1>アカウントが認証されました！</h1>
  </div>
  <div class="general_field">
    <h4>
      ブラウザアプリからログインしてください<br>
      (メールアプリからは利用できません)
    </h4>
  </div>
  <div class="general_field">
    <h2>すぐに出会いましょう</h2>
    <button onclick="copyUrl()", id="copyButton">ログイン画面のURLをコピー</button>
  </div>
 
</div>


